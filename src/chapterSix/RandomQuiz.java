package chapterSix;

import java.util.Scanner;

public class RandomQuiz {
    /*private static int correctAnswer;
    private static int numberOfCorrectAnswers;
    private static int numberOfWrongAnswers;

    public static void main(String[] args){
        //run quiz
        //do all of these 10 lines
        //generate questions
        runQuiz();
        outputPerformance();
        //collect user input
        //mark question
        //calculate score
        //output performance
    }

    private static void runQuiz() {
        int count = 0;
        while(count<10) {
            String question = generateQuestions();
            //int userInput = collectUserInputFor(question);
            //markQuestion(userInput);
            count++;
        }
    }

    private static void outputPerformance() {
        System.out.println("Number of correct answers is "+ numberOfCorrectAnswers);
        System.out.println("Number of wrong answers is "+ numberOfWrongAnswers);
        System.out.println("Adjusted score is "+ (numberOfCorrectAnswers - numberOfWrongAnswers));
    }

    private static void markQuestion(int userInput) {
        if(userInput == correctAnswer) numberOfCorrectAnswers++; else numberOfWrongAnswers++;
    }

    //private static int collectUserInputFor(String question) {
        Scanner giannis = new Scanner(System.in);

    //}

    private static String generateQuestions() {
        String question = "";
        // int firstNumber = generateNumber();
       // int secondNumber = generateNumber();
        //char operator = generateOperator();
        String space = " ";
        //calculateAnswer(firstNumber, operator, secondNumber);
        //question = firstNumber +space + operator + space + secondNumber + space + "=" ;
        return question;
    }

    private static void calculateAnswer(int firstNumber, char operator, int secondNumber) {
    }

    //private static char generateOperator() {
    }

    //private static int generateNumber() {
       // return
    //}*/
}
